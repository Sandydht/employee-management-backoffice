import { CurrencyFormatDirective } from './currency-format';

describe('CurrencyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
